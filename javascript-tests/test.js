window.onload = function () {
    const numbers = [1, 2, 3];

    // Many lines of code

    function foo() {
        console.log('foo');
    }

    const myObject = {
        width: 2,
        bar: function() {
            console.log('bar');
        }  
    }
}